<%= form_with model: @link, remote: true do |form| %>
	<div id= "post_errors" style="display:none"></div>
	<%= form.text_field :original_url,  placeholder: "Enter URL..." %>
	<%= form.submit "Apply", class: "btn btn-primary mt-3" %>
<% end %>